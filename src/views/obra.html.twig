 {% extends 'layout.html.twig' %}

{% block title %}Obras{% endblock %}

{% block contenido %}
   <li>
        <h3>{{obra.titulo}} , ({{obra.anho}})</h3>
        <p>{{obra.descripcion}}</p>
        <p>
        	{% for artista in obra.artistas%}
        	 <a href="{{path('artista',{id: artista.id}) }}" >{{artista.nombre}}</a>
        	{% endfor %}
        </p>
  </li>
  
  
  
{% endblock %}
